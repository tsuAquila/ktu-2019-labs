#include <stdio.h>

void main () 
{
    int arr[20], n, elt, i, t;

    printf("Enter number of elements: ");
    scanf("%d", &n);
    t = 0;

    printf("Enter elements: ");
    scanf("%d", &arr[t]);
    for ( i = 1; i < n; i++)
    {
        for (int j = 0; i <= t ; j++)
        {
            
        }
        
    }
    
}