#include <stdio.h>

void main()
{
    char infix[10], postfix[10];
    printf("Enter infix expression: ");
    scanf("%s", infix);
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
}